PreCommit:
  RuboCop:
    enabled: true
    command: ['bundle', 'exec', 'rubocop']
  ShellCheck:
    enabled: true
    include:
      - 'bin/bootkit'

CommitMsg:
  # Conflicts with the conventional-commit prefixes used in this repo
  CapitalizedSubject:
    enabled: false
